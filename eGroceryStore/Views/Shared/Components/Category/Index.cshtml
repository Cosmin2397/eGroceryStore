@model List<Category>

    @foreach(var item in Model)
    {
        <a class="list-group-item list-group-item-action" asp-controler="Products" asp-action="GetCategoryProducts" asp-route-id="@item.Id" style="font-size: 16px;">@item.Name</a>
    }